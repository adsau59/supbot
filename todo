

Bugs:
multiple name tagging reference
if image is not found bot will pause
if loading chat takes time to load, when goint to other chat, it reads all the chat as new
actionmanager popandperform gives nosuchelement  exception

Later:
Logging to file
load strings dynamically instead of static final
natural language recognition

Done:
Databse
Configuration
ConsoleCommands
ChatProcessor
add Checker with timeout
scheduling
add sheduletask at specific time
networking
add action from network
alias
(done but will add later externally)
Image Actions
action which downloads image
action which uploads image

small changes
better command-arg splitting (qoutes)
way to configure command prefix from api
register bug

bugs fixed:
when a new message from other group comes, the group shifts to top which changes the webelement
